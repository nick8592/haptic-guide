[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (requires model download)
addopts = -v --tb=short

[tool:pytest.markers]
slow = "marks tests as slow (requires model download)"
